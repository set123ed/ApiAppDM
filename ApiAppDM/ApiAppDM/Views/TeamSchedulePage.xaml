<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ApiAppDM.Views.TeamSchedulePage"
    Title="Coaches 2020" >

    <ContentPage.Content>
        <ListView x:Name="ListView1" ItemsSource="{Binding CoachesInfo}" HasUnevenRows="True" IsVisible="{Binding Connected}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid HorizontalOptions="Start" Margin="30" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>

                        </Grid.RowDefinitions>
                        <Label Text="{Binding firstName}"  HorizontalOptions="EndAndExpand" Grid.Row="0" TextColor="Black"/>
                        <Label Text="{Binding lastName}" HorizontalOptions="EndAndExpand" Grid.Row="1" TextColor="Black" />
                        <Label Text="{Binding college}" HorizontalOptions="EndAndExpand" Grid.Row="2" TextColor="Black" />
                    </Grid>

                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        
    </ContentPage.Content>
</ContentPage>
